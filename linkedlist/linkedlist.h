#define LINKEDLIST_H_

namespace linkedlist{ 

class LinkedList {
  private:
    LinkedList(data);
    Node *head;
    Node *tail;
    int size;
    void append();
    void prepend();
    void insert();
    
}
} // namespace linkedlist
